# 🎨 Cartoonify Images using Django & PyTorch

This project allows users to upload a real image and convert it into a **cartoon-style image** using a deep learning model built with **PyTorch** and served through a **Django web app**.

---

## 🚀 Features
- Upload real-world photos and generate cartoon versions instantly  
- Uses a trained **Generator network (GAN)** model  
- Clean, minimal **Django UI** with image preview  
- Real-time inference (CPU/GPU supported)

---

## 🧠 Tech Stack
- **Frontend:** HTML, CSS, Bootstrap (via Django Templates)  
- **Backend:** Django 5.x  
- **Deep Learning:** PyTorch  
- **Image Processing:** Pillow, Torchvision  
- **Deployment:** Localhost (can be extended to Render/Heroku)

---

## ⚙️ Setup Instructions

### 1️⃣ Clone this repository
```bash
git clone https://github.com/ssonammm03/cartoonify_images.git
cd cartoonify_images
